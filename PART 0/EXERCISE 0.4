. The browser loads and fetches the HTML code with the HTTP GET request from the server which defines the structure of the page(https://studies.cs.helsinki.fi/exampleapp/notes)
. The HTML code is displagd with a status of 200
. The links in the HTML code fetched from the server causes the browser to fetch the css code with the HTTP GET request(https://studies.cs.helsinki.fi/exampleapp/main.css). 
. the CSS and and the HTML is displayed with the status code 200.
. The links in the CSS code fetched from the server causes the browser to fetch the javascript file from the server with the HTTP GET request(https://studies.cs.helsinki.fi/exampleapp/main.js)
. The javascript code starts and run and is diplayed on the browser.
. The input box is clicked and has value inputed in it. When the submit button is clicked the browser makes a request to create a new not with the HTTP POST request(https://studies.cs.helsinki.fi/exampleapp/new_note)
. The browser reloads and displays the page from the json file. 