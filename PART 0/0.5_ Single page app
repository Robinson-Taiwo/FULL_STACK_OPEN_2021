. The browser requests the single HTML page from the server with the HTTP GET  request ()
.the browser displays the html code with the status code 200
. The browser requests the javascript file from the server with the HTTP GET request
. The  browser then renders the javascript file
. The browser requests the data(json file) from the server with the HTTP GET request
.The browser executes the javascript code that manipulates the output of the HTML from the json file