. The browser takes the input and request for the javascript code with the HTTP GET request
. the server fetches the javascript code and renders it to the browser.
. The javascript code executes the following commands:
# It prevents the browser from running in default
# It creates a new note file and sends it to the list in json file
# It renders the lists from the json file to the browser
# It sends the note to the server with the HTTP POST request.
. The server returns the code to the browser with the status code 201(created).